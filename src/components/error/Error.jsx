import React from 'react'
import { ErrorContainer, ErrorText } from '../../styles/ErrorStyle'

const Error = () => {
  return (
    <ErrorContainer>
      <ErrorText>This Page failed to load</ErrorText>
    </ErrorContainer>
  )
}

export default Error